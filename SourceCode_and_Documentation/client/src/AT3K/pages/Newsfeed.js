import React from "react";
import PropTypes from "prop-types";

const Newsfeed = (props) => {
    return (
        <div>
            <h1>Newsfeed</h1>
        </div>
    );
};

Newsfeed.propTypes = {};

export default Newsfeed;
